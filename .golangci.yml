version: "2"

linters:
  settings:
    errcheck:
      # Writes to already-open stdout/stderr streams have no actionable error;
      # excluding them keeps errcheck focused on I/O that can genuinely fail.
      exclude-functions:
        - fmt.Fprint
        - fmt.Fprintf
        - fmt.Fprintln
        - io.WriteString
